from architecture.code_analysis_v2.core.controller import ControllerStudent, ControllerTeacher
import logging

LOGGER = logging.getLogger(__name__)


def main():
    # TODO 5. Initially, the university will have 3 courses with 3 subjects each.
    # map each command in controllers to a command to the user, like `tdd student subscribe 1234`
    # the command above calls the method `ControllerStudent.subscribe_to_subject` with the arguments
    raise NotImplementedError
